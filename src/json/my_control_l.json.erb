{
    "title": "Personal rules (@kx1)",
    "rules": [
        {
            "description": "Change left_control + @ to escape",
            "manipulators": [
                {
                    "type": "basic",
                    "from": <%= from("open_bracket", ["left_control"], ["any"]) %>,
                    "to": <%= to([["escape"]]) %>
                }
            ]
        }
    ]
}
